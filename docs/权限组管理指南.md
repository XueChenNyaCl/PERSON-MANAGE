# 权限组管理指南

## 概述
本系统采用LuckPerms风格的权限管理系统，支持通配符权限、优先级覆盖和细粒度权限控制。权限系统默认没有任何权限，需要通过API手动添加。

## 权限系统架构

### 数据库结构
系统使用两个主要表存储权限：

1. **permissions表** - 存储角色权限节点
   - `id` UUID 主键
   - `role` VARCHAR(50) 角色名称（admin, teacher, student, parent）
   - `permission` VARCHAR(255) 权限节点
   - `priority` INTEGER 优先级（值越大优先级越高）
   - `created_at` TIMESTAMP 创建时间
   - `updated_at` TIMESTAMP 更新时间

2. **user_permissions表** - 存储用户特定权限
   - `id` UUID 主键
   - `user_id` UUID 用户ID
   - `permission` VARCHAR(255) 权限节点
   - `value` BOOLEAN 权限值（true=允许，false=拒绝）
   - `priority` INTEGER 优先级（默认100，高于角色权限）
   - `created_at` TIMESTAMP 创建时间
   - `updated_at` TIMESTAMP 更新时间

### 权限节点格式
权限节点采用点分隔的字符串表示，支持以下格式：

1. **标准权限节点**：`module.action.target`
   - 示例：`person.view`、`class.update_teacher`

2. **通配符权限**：使用`*`匹配任意部分
   - 示例：`person.*` 匹配所有人员相关权限
   - 示例：`*.view` 匹配所有查看权限
   - 示例：`*.*` 匹配所有权限（慎用）

3. **否定权限**：以`-`开头表示拒绝权限
   - 示例：`-person.delete` 拒绝删除人员权限
   - 注意：否定权限优先级高于普通权限

### 权限评估规则
1. **优先级系统**：权限按优先级排序，优先级高的权限覆盖优先级低的权限
2. **继承关系**：用户权限继承自角色权限，用户特定权限可以覆盖角色权限
3. **否定权限**：否定权限（以`-`开头）直接拒绝访问，优先级高于允许权限
4. **通配符匹配**：通配符权限可以匹配多个具体权限
5. **默认行为**：如果没有匹配的权限节点，默认拒绝访问

## 细化的权限结构

### 权限设计原则
1. **单一职责**：一个权限只对应一种操作，不能对应一片操作
2. **通配符覆盖**：只有`权限名.*`才能拥有该权限的所有操作
3. **层级结构**：权限采用`模块.操作.目标`的三级结构
4. **以后端实际校验为准**：前端菜单、文档别名与后端接口校验不一致时，应以后端源码中的实际检查权限为准

### 后端实际校验补充说明

根据后端当前实现，除通用权限外，还存在以下需要特别注意的权限形式：

1. **AI 模块权限**
  - `ai.chat`：AI 对话能力
  - `ai.settings`：AI 设置管理

2. **班级教师变更权限命名**
  - 后端 `backend/src/api/class.rs` 实际检查的是 `class.update_teacher`
  - 当前权限翻译接口和部分文档条目中出现的是 `class.update.teacher`
  - **实际授权时建议优先配置 `class.update_teacher`**，避免接口鉴权失败

3. **班级特定权限后缀**
  - 后端支持将班级 ID 后 6 位作为权限后缀
  - 例如：`class.ab12cd`、`group.view.ab12cd`、`group.create.ab12cd`
  - 这类权限用于给教师分配“仅限某个班级”的小组管理权限

### 人员权限 (person)
| 权限节点 | 说明 |
|---------|------|
| `person.view` | 查看人员列表 |
| `person.view.detail` | 查看人员详情 |
| `person.sensitive.view` | 查看敏感信息（学号、电话等） |
| `person.create` | 创建人员 |
| `person.update` | 更新人员信息 |
| `person.update.status` | 更新人员状态 |
| `person.delete` | 删除人员 |
| `person.*` | 所有人员权限（通配符） |

### 班级权限 (class)
| 权限节点 | 说明 |
|---------|------|
| `class.view` | 查看班级列表 |
| `class.view.detail` | 查看班级详情 |
| `class.create` | 创建班级 |
| `class.update` | 更新班级信息 |
| `class.update.name` | 修改班级名称 |
| `class.update.grade` | 修改班级年级 |
| `class.update.teacher` | 修改班主任（文档/翻译中的点分形式） |
| `class.update_teacher` | 修改班主任（后端接口实际检查使用） |
| `class.delete` | 删除班级 |
| `class.{class_suffix}` | 某个班级的通用管理权限，`{class_suffix}` 为班级ID后6位 |
| `class.*` | 所有班级权限（通配符） |
| `class.manage` | 管理班级

### 部门权限 (department)
| 权限节点 | 说明 |
|---------|------|
| `department.view` | 查看部门 |
| `department.create` | 创建部门 |
| `department.update` | 更新部门 |
| `department.delete` | 删除部门 |
| `department.*` | 所有部门权限（通配符） |

### 考勤权限 (attendance)
| 权限节点 | 说明 |
|---------|------|
| `attendance.view` | 查看所有考勤 |
| `attendance.view.own` | 查看自己的考勤 |
| `attendance.create` | 创建考勤记录 |
| `attendance.update` | 更新考勤 |
| `attendance.delete` | 删除考勤 |
| `attendance.*` | 所有考勤权限（通配符） |

### 成绩权限 (score)
| 权限节点 | 说明 |
|---------|------|
| `score.view` | 查看所有成绩 |
| `score.view.own` | 查看自己的成绩 |
| `score.create` | 创建成绩 |
| `score.update` | 更新成绩 |
| `score.delete` | 删除成绩 |
| `score.*` | 所有成绩权限（通配符） |

### 通知权限 (notice)
| 权限节点 | 说明 |
|---------|------|
| `notice.view` | 查看通知 |
| `notice.create` | 创建通知 |
| `notice.update` | 更新通知 |
| `notice.delete` | 删除通知 |
| `notice.*` | 所有通知权限（通配符） |

### 系统权限 (system)
| 权限节点 | 说明 |
|---------|------|
| `system.settings` | 系统设置 |
| `system.permissions` | 权限管理 |

### 仪表板权限 (dashboard)
| 权限节点 | 说明 |
|---------|------|
| `dashboard.view` | 查看仪表板 |

### AI 权限 (ai)
| 权限节点 | 说明 |
|---------|------|
| `ai.chat` | 使用 AI 对话能力 |
| `ai.settings` | 管理 AI 设置 |

### 小组权限 (group)
| 权限节点 | 说明 |
|---------|------|
| `group.view` | 查看小组列表 |
| `group.create` | 创建小组 |
| `group.update` | 更新小组信息 |
| `group.delete` | 删除小组 |
| `group.update.member` | 管理小组成员 |
| `group.update.score` | 管理小组积分 |
| `group.view.{class_suffix}` | 查看指定班级的小组列表 |
| `group.create.{class_suffix}` | 在指定班级创建小组 |
| `group.update.{class_suffix}` | 更新指定班级的小组信息 |
| `group.delete.{class_suffix}` | 删除指定班级的小组 |
| `group.update.member.{class_suffix}` | 管理指定班级的小组成员 |
| `group.update.score.{class_suffix}` | 管理指定班级的小组积分 |
| `group.*` | 所有小组权限（通配符） |

## 预定义权限组

### 管理员权限组 (admin)
管理员拥有所有权限，包括：
- 所有查看权限
- 所有创建权限
- 所有更新权限
- 所有删除权限
- AI 权限：`ai.chat`, `ai.settings`
- 系统权限：`system.settings`, `system.permissions`
- 通配符权限：`person.*`, `class.*`, `department.*`, `attendance.*`, `score.*`, `notice.*`, `group.*`

### 教师权限组 (teacher)
教师可以管理自己班级的考勤、成绩和小组，但不能：
- 创建/修改/删除人员
- 创建/修改/删除班级（可以修改班主任）
- 创建/修改/删除部门
- 删除考勤记录
- 删除成绩
- 管理通知

教师拥有的小组权限：
- `group.view`
- `group.create`
- `group.update`
- `group.delete`
- `group.update.member`
- `group.update.score`

后端还支持给教师分配班级范围权限，例如：
- `class.{class_suffix}`
- `group.view.{class_suffix}`
- `group.create.{class_suffix}`
- `group.update.{class_suffix}`
- `group.delete.{class_suffix}`
- `group.update.member.{class_suffix}`
- `group.update.score.{class_suffix}`

### 学生权限组 (student)
学生只能查看，不能修改任何信息：
- 查看人员列表（不能查看详情和敏感信息）
- 查看班级列表（不能查看详情）
- 查看自己的考勤
- 查看自己的成绩
- 查看通知
- 查看小组列表

### 家长权限组 (parent)
家长只能查看子女信息，不能修改：
- 查看人员列表（子女信息）
- 查看班级列表（子女所在班级）
- 查看子女的考勤
- 查看子女的成绩
- 查看通知
- 查看小组列表（子女所在小组）

## API接口说明

### 权限管理API

#### 1. 获取所有角色权限
```
GET /api/permissions
```
**权限要求**：`system.settings`

**响应示例**：
```json
[
  {
    "role": "admin",
    "permissions": [
      {
        "permission": "person.view",
        "priority": 10,
        "created_at": "2026-02-26T10:30:00Z"
      }
    ]
  }
]
```

#### 2. 添加角色权限
```
POST /api/permissions
```
**权限要求**：`system.settings`

**请求体**：
```json
{
  "role": "teacher",
  "permission": "class.*",
  "priority": 5
}
```

#### 3. 移除角色权限
```
DELETE /api/permissions
```
**权限要求**：`system.settings`

**请求体**：
```json
{
  "role": "teacher",
  "permission": "class.delete"
}
```

#### 4. 检查权限
```
POST /api/permissions/check
```
**权限要求**：无（当前用户）

**请求体**：
```json
{
  "permission": "person.view"
}
```

**响应**：
```json
{
  "has_permission": true,
  "result": "allowed"
}
```

### 用户权限管理API

#### 1. 获取用户特定权限
```
GET /api/permissions/users/{user_id}
```
**权限要求**：管理员或用户本人

#### 2. 添加用户特定权限
```
POST /api/permissions/users/{user_id}
```
**权限要求**：管理员或用户本人

**请求体**：
```json
{
  "permission": "-person.delete",
  "value": false,
  "priority": 150
}
```

#### 3. 移除用户特定权限
```
DELETE /api/permissions/users/{user_id}?permission=person.delete
```
**权限要求**：管理员或用户本人

## 通配符权限示例

### 常用通配符模式
1. **`module.*`** - 模块所有权限
   - `person.*` - 所有人员权限
   - `class.*` - 所有班级权限

2. **`*.action`** - 所有模块的特定操作
   - `*.view` - 所有查看权限
   - `*.create` - 所有创建权限
   - `*.update` - 所有更新权限
   - `*.delete` - 所有删除权限

3. **`module.action.*`** - 模块操作的所有目标
   - `person.update.*` - 更新所有人员属性

### 通配符与否定权限结合
```yaml
# 允许所有查看权限
*.view

# 但拒绝查看敏感信息
-person.sensitive.view

# 允许所有班级权限
class.*

# 但拒绝删除班级
-class.delete
```

## 权限优先级示例

### 优先级覆盖示例
```yaml
# 角色权限（优先级10）
person.view = true

# 用户特定权限（优先级100）
-person.view = false

# 最终结果：用户无法查看人员（用户权限覆盖角色权限）
```

### 否定权限优先级
```yaml
# 通配符允许（优先级5）
person.* = true

# 具体拒绝（优先级10）
-person.delete = false

# 最终结果：用户有所有人员权限，但不能删除
```

## 最佳实践

### 1. 最小权限原则
- 默认不给任何权限
- 按需添加权限，而非一开始给全部权限
- 使用通配符简化管理，但要注意范围

### 2. 权限分组管理
- 按角色创建权限组
- 使用通配符减少重复配置
- 定期审查和清理无用权限

### 3. 权限测试
- 使用`/api/permissions/check`接口测试权限
- 在生产环境变更前先在测试环境验证
- 记录权限变更日志

### 4. 安全注意事项
- 否定权限优先级高，谨慎使用
- 通配符`*.*`会授予所有权限，慎用
- 用户特定权限优先级最高，注意管理

## 初始化脚本
系统提供了`run_permission_migration.rs`脚本用于初始化权限表。默认情况下，脚本会创建空表，需要手动添加权限。

如需初始化默认权限，可以取消注释脚本中的`init_default_permissions`函数调用。

## 故障排除

### 常见问题
1. **权限不生效**：检查权限优先级和否定权限
2. **通配符不匹配**：确认权限节点格式正确
3. **数据库错误**：检查权限表是否存在

### 调试方法
1. 使用`/api/permissions/check`接口测试权限
2. 查看数据库中的权限记录
3. 检查用户角色是否正确
4. 核对后端实际接口检查的权限名是否与配置完全一致，尤其注意：
  - `class.update_teacher` 与 `class.update.teacher`
  - `ai.chat` 与前端菜单中的 `ai.view`
  - `group.*.{class_suffix}` / `class.{class_suffix}` 这类班级特定权限

## 版本历史
- v1.0 (2026-02-26)：初始版本，基于LuckPerms风格实现动态权限系统
- v1.1 (2026-02-26)：添加通配符支持和否定权限功能
- v2.0 (2026-02-27)：细化权限结构，每个权限只对应一种操作
- v2.1 (2026-03-09)：根据后端实际权限检查补充 AI 权限、班级特定权限及缺失权限键说明
